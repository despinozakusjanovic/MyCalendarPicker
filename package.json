{
  "name": "rn-calendar-picker",
  "version": "1.0.0",
  "description": "Component for React Native",
  "engines": {
    "node": ">=4.0.0"
  },
  "peerDependencies": {
    "date-fns": "*",
    "react": "*",
    "react-native": "*"
  },
  "main": "./index.js",
  "scripts": {
    "start": "echo \"example folder contains sample app (see README)\" && exit 1",
    "test": "echo \"example folder contains the tests (see README)\" && exit 1",
    "lint": "eslint MyCalendarPicker --fix",
    "postinstall": "node-git-hooks"
  },
  "keywords": [
    "calendar",
    "picker",
    "calendar",
    "picker",
    "date",
    "picker",
    "day",
    "picker"
  ],
  "directories": {},
  "dependencies": {
    "node-git-hooks": "^1.0.1",
    "prop-types": "^15.6.0",
    "recyclerlistview": "^3.0.0"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "date-fns": "^3.1.0",
    "eslint": "^6.8.0",
    "eslint-plugin-react": "^7.19.0"
  }
}